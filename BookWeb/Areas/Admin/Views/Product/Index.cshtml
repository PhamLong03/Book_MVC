@model List<Product>

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Product List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Create new product</a>
        </div>
    </div>

    <table id="tblTable" class="table table-striped mb-5">
        <thead>
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>ISBN</th>
                <th>Author</th>
                <th>Category</th>
                <th>List price</th>
                <th>Price</th>
                <th></th>
            </tr>
        </thead>
        @* @foreach(var product in Model)
        {
            <tr>
                <td>@product.Title</td>
                <td style="max-width:300px; min-width:300px">@product.Description</td>
                <td>@product.ISBN</td>
                <td>@product.Author</td>
                <td>@product.Category.Name</td>
                <td>@product.ListPrice</td>
                <td>@product.Price</td>
                <td>@product.Price50</td>
                <td>@product.Price100</td>
                <td>
                    <div class="btn-group w-75" role="group">
                        <a asp-controller="Product" asp-action="Upsert" asp-route-Id="@product.Id" class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i>Edit</a>
                        <a asp-controller="Product" asp-action="Delete" asp-route-Id="@product.Id" class="btn btn-danger mx-2"><i class="bi bi-x-square-fill"></i>Remove</a>
                    </div>
                </td>
            </tr>
        } *@
    </table>
</div>

                    

@section Scripts{
    <script src="~/js/product.js"></script>
}